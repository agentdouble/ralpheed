[project]
name = "ralph-backend"
version = "0.1.0"
description = "Ralph integration API"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = []
