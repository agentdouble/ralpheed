# Backend
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# Frontend (Vite)
FRONTEND_HOST=127.0.0.1
FRONTEND_PORT=5173

# Optional: comma-separated list. If set, overrides FRONTEND_HOST/PORT.
# CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
CORS_ORIGINS=
